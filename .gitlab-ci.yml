default:
  image: docker:latest
  services:
    - docker:dind

variables:
  project_name: MyProjectName
  SEMANTIC_RELEASE_PACKAGE: ${project_name}

stages:
  - build
  - release

build-supplementary-image:
  stage: build
  script:
    - docker login registry.gitlab.com -u ujlbu4 -p ${GITLAB_TOKEN}
    - docker pull registry.gitlab.com/ujlbu4/gitlab-semantic-release
    - docker build -t registry.gitlab.com/ujlbu4/gitlab-semantic-release .
    - docker push registry.gitlab.com/ujlbu4/gitlab-semantic-release
  only:
    - master
  except:
    refs:
      - tags
    variables:
      - $CI_COMMIT_TITLE =~ /^RELEASE:.+$/

demo:
  stage: build
  script:
    - echo "demo"

release:
  image: registry.gitlab.com/ujlbu4/gitlab-semantic-release
  stage: release
  script:
    - env
    - npx semantic-release
  only:
    - master
  except:
    refs:
      - tags
    variables:
      - $CI_COMMIT_TITLE =~ /^RELEASE:.+$/
